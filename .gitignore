# --- Sensitive Active Directory artifacts: never commit ---
*.dit
ntds.dit
SYSTEM
SECURITY
*.hive
*.hiv

# --- ESE transaction/metadata files (can hold raw page images with secrets) ---
edb*.log
edb.chk
*.jfm
*.jrs
temp.edb

# --- Dictionaries / wordlists (cleartext candidate passwords, incl. the canary's) ---
weak*.txt
*wordlist*
*dict*.txt
wordlists/

# --- Extraction output (contains password hashes) ---
*.hashes
*.ntds
report*.json
report*.csv
audit*.json
audit*.csv

# --- OS / editor noise ---
.DS_Store
Thumbs.db
*.swp
*~
